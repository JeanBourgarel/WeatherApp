<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.weather.home.HomeFragment"
        android:label="fragment_home">
        <action
            android:id="@+id/launch_nextDaysFragment"
            app:destination="@id/nextDaysFragment" />
    </fragment>
    <fragment
        android:id="@+id/nextDaysFragment"
        android:name="com.weather.nextdays.WeatherNextDays"
        android:label="fragment_next_days"
        tools:layout="@layout/fragment_weather_next_days" />
</navigation>